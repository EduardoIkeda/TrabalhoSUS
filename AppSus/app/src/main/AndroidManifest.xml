<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppSus"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.RegisterUserActivity"></activity>
        <activity android:name=".Activities.ConsultasActivity"></activity>
        <activity android:name=".Activities.SpecialitiesActivity"></activity>
        <activity android:name=".Activities.HealthCentersActivity"></activity>
        <activity android:name=".Activities.HourActivity"></activity>
        <activity android:name=".Activities.AppointmentConfirmationActivity"></activity>
        <activity android:name=".Activities.DateActivity"></activity>
        <activity android:name=".Activities.ProfileActivity"></activity>

    </application>

</manifest>